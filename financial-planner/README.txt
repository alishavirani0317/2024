FINANCIAL-PLANNER/
├── go-service/       → Your main app (backend server)
│   ├── db/           → Your data setup (e.g. expense structure)
│   ├── routes/       → Code that handles user requests (like "Add Expense")
│   └── main.go       → The file that runs the whole server
├── python-logic/     → Your smart tools (math & graphs)
    ├── analyze.py    → Figures out how much money you’re spending
    └── graphs.py     → Makes graphs and charts from your expenses
    